# 阶段 3-2b 评审报告（节点图编辑器体验完善）

日期：2026-09-19 · 范围：a8aaa32..0c9ad0a · 结论：**无 P0；P1×4 全部修复（0c9ad0a），237/237 测试全绿，验收通过——节点图编辑器达到完整形态，进入 3-3 代码页批次**

## 1. 本批交付（F1-F7）

- **F1 运行日志**：SessionHost 可选钩子 `onLogicEvent`（LevelSession 对 GraphEngine onError 与 handleCommand 命令轨迹双路收集）；LevelRunner 按 levelId 写 sessionStorage（最近 50 条）；节点图页底部可折叠面板，点击带 nodeId 的错误 → `rf.setCenter` 定位并选中节点
- **F2 变量面板**：左栏底部（graphProgram 版函数：ID_RE 校验、rename 同步改写 assign.target；初值 parseScalarInput 启发式 + JSON 解析；改名失败还原输入）
- **F3 lint 问题列表**：工具条计数可点展开（code/消息），点击定位节点或边；Inspector datalist 补全 21 个白名单函数（`fn(` 形式）
- **F4 查看脚本**：工具条开关 → generateScript 只读覆盖层；非结构化图显示错误消息本身；补 `.script-view` 样式（GraphPage 顺手修复）
- **F5 模板**：「计分初始化与门槛结算」（通用）与「防卡死守卫」（需 timer 组件，引用真实实例 id）两组一键落图；`applyTemplate` 纯函数（newNodeId 递推 + 索引边映射 + requiredVars 自声明）；落图后 lint 全绿
- **F6 EditorPage 装载**：loadedIdRef 修复**真实落地**（docs/15 曾声称但未落地）：同 id 不重装（挡住 liveQuery 回写覆盖保存后编辑）、换 id 强制重装
- **F7 杂项**：左栏库搜索框；gmenu 屏幕边缘钳制；lint 孤儿节点检查（非 on 无入边 → structure「执行流到不了这里」；迁移产物零误报已验证）

## 2. 评审发现与修复（0c9ad0a）

| 级别 | 问题 | 修复 |
| --- | --- | --- |
| P1-1 | 变量值失焦**无条件启发式回写**：内置关卡的数组初值（rhythm-follow `taps: []`、melody-dictation `typed: []`）在普通「点进→点开」交互下被静默毁成字符串，运行时 `append` 必抛 | 未变更失焦 no-op（比较 JSON 快照）；`[`/`{` 开头走 JSON.parse（失败还原输入框）；补 3 个 jsdom 回归 |
| P1-2 | 改名失败的还原 catch 是**死代码**（apply 吞错转错误条永不 throw）——非法/撞名后输入框停留非法文本 | `apply` 返回 boolean，面板透传结果，失败还原输入框；jsdom 回归 |
| P1-3 | 运行日志无「次」边界：多次试运行/重开混写同一 key，「最近一次」承诺失真 | LevelRunner 装载 effect 在 start 前 clearRunLog(levelId) |
| P1-4 | **命令执行失败不进日志**：call 节点真正的失败（未知实例/未知命令——「点了没反应」的主因）在 handleCommand catch 里只进 console | GraphHost.dispatchCommand 增加 `context?: { nodeId, event }`（execNode 现成有 node.id），LevelSession catch 时发 error 事件（带 nodeId 可定位）；红测钉死 |
| P2 已修 | 日志计数只在面板展开时可见（注意力信号错位）→ 无条件读取；dangling 边定位回退终点；模板 requiredVars 自声明（已删 score 的关卡落模板自动补声明，lint 保持全绿） | 已落地 |
| P2 记录 | focusNode 按 NODE_H 常量垂直居中（高卡视觉瑕疵）；孤儿告警链式刷屏（链头置顶待做）；换 id/后退丢弃未保存编辑（key={hash} 全局行为，dirty-guard 待办 3-3 前）；appendRunLog 逐事件全量写（≤50 条可忽略）；quota 超限静默丢当条 | 记录在 docs，按需后置 |

## 3. 验证

- `tsc --noEmit` 干净；`vitest run` **237/237**（新增 runLog 3 例、模板 3 例、LevelSession 钩子 2 例、VariablesPanel 4 例、孤儿 lint 1 例）；`npm run build` 通过
- 浏览器验收：变量改名 `score→points` → assign 卡片同步 `v.points =` + lint 出现 4 条 undeclared-var → 问题列表展开 → 点击第一条定位到 `check-answer_d3` 并在 Inspector 显示 field 级错误；改回 `score` → 问题清零；模板一键落图 +5 节点 +3 边 lint 全绿；查看脚本覆盖层正常/关闭；**运行日志完整链**——新建关卡落模板 → branch 条件改为 `1 +`（lint 警告不拦保存）→ 试运行触发 → sessionStorage 记录 `{kind:'error', message:'表达式意外结束', nodeId:'n6', event:'level.questionLoaded'}` → 编辑器日志面板显示 → 点击定位选中 n6（红卡 + Inspector field 错误）；A→B 直跳换文档

## 4. 结论

**3-2b 验收通过，节点图编辑器达到完整形态**：结构编辑（信息卡/端口/校验/布局/位置持久化）+ 变量面板 + 模板 + lint 定位 + 脚本只读视图 + 运行日志闭环。评审确认主链路（日志时序/模板原子性/focusNode/孤儿规则/装载守卫）实现正确。**进入 3-3（LevelScript 代码页：生成+编辑+解析应用+行级报错+脏提示）**——脚本视图与变量面板同屏共事，3-2b 的数据完整性修复正是其前置。
