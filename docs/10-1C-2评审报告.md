# 阶段 1C-2 评审报告（编辑器完善）

> 日期：2026-09-17 · 范围：`63088f9..HEAD`（6 提交：alto/clef-test、2-1 组件批、编辑器完善批、若干修复）
> 方式：subagent 聚焦 code review + 浏览器验收（变量表增删、表达式实时报错/修正消失、内置保存拦截、favicon 与画布布局）。
> 结论：初版**可合入，无 P0**；1 个 P1（rename 撞名静默覆盖）与 P2 若干已修复，剩余记录在案。

## 总评

runId 重排、emit 稳定引用、保存拦截、clef 三处一致等核心机制全部正确；本轮补齐的变量管理 UI 与表达式实时校验把编辑器从"能写 JSON 的画布"推进为"可核对语法的创作工具"。评审发现的 P1 是 renameVariable 撞名静默覆盖（逐键改名中间态极易触发）——已加守卫。

## 发现与修复状态

- **[P1] ✅ 已修复** renameVariable 撞名静默覆盖（vars[newName]=vars[oldName] 吞掉目标初值；逐键改名中间态即触发）→ RulesEditor renameVar 撞名守卫拒绝 + docState 防御 + 测试
- **[P2] ✅ 已修复** RulesEditor/QuestionsEditor onChange 统一改传 update（修改即清「已保存」提示）；schemas.test 补 clef-test/rhythm-follow/tune-intro 三样本；playClick 改为返回按轮停用函数（RhythmView 收集 disposers、cleanup 逐一静停，替代全局 stopAllClicks 集合，消除多 rhythm 组件误杀面）
- **[P2] ✅ 已修复（前批遗留）** App 页面组件按 hash 加 key——修复 #/edit/A→#/edit/B 导航时 EditorPage 保留 A 的 doc 状态、把 B 保存进 A 记录的串号隐患（e1bc978）
- **[P3] ⏳ 记录**：改名逐键 trim 无法输入含空格名称；parseScalarInput 使字符串 "5" 再编辑静默翻 number；LinesField firstError 不短路（规模无碍）；表盘 ±5¢ 变绿与判定 ±50¢ 门槛不一致；tune-intro 未校验目标音高、stable 按帧计非严格墙钟 1 秒
- **[P2 测试] ⏳ 部分补齐**：编辑器 UI jsdom 测试（RulesEditor 渲染/受控回写/LinesField 校验显隐）已入库；HomePage/SeriesPage 页面级测试随 1C 迭代三

## 已核查无问题（摘要）

runId 时序（React 保证旧 cleanup 先于新 effect，stopAllClicks/按轮 disposers 不会误杀新轮）；emitFor 稳定引用防视图扰动重排；checkExprText 纯语法检查与首错选取；actionKind 与引擎判定序一致；`__` 前缀命令过滤；保存拦截 builtIn 匹配与 tryRun await；JsonTab 强制保留 id 与同步；App key=hash 无误重挂载；clef alto 三处一致；rhythm-follow 每题 grid 步长与 BPM 吻合、judge 后清空 taps、失败重开带新 runId。

## 本阶段验证

- vitest **130/130**（本批新增：变量操作 4、编辑器 UI jsdom 4、rhythmScore 1、schema 三新样本 3、组件 id 唯一性回归 1 等）
- tsc strict ✅ · vite build ✅
- 浏览器验收：变量表增删、表达式实时报错/修正消失、内置保存拦截提示、favicon 与画布主体布局
