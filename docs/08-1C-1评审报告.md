# 阶段 1C-1 评审报告（关卡编辑器 MVP）

> 日期：2026-09-17 · 范围：`dbff810..HEAD`（编辑器 MVP：src/editor/ 全部、LibraryPage/App 增量、public/editor-config.json）
> 方式：subagent 聚焦 code review + 浏览器验收（新建→面板加组件→JSON 灌配置→保存→试运行→进度归档）。
> 结论：**可合入**，无 P0；3 个 P1 与 2 个 P2 已在本批修复（见文末状态）。

## 总评

结构清晰、数据流设计合理：docState 纯函数 + EditorPage 薄 UI、组件类型与事件/命令枚举以注册表契约为单一来源、保存链（loadLevelDoc 校验 → putResource）与试运行链（保存后跳运行页）闭环。拖拽核心（pointer capture + dragRef.id 守卫 + touch-action）与 React 受控输入模式经核查无误。

## 发现与修复状态

- **[P1-1] ✅ 已修复** 删除组件后再添加同类型生成重复 id（`${type}${length+1}` 按长度生成）→ 查重递增；规则/题目 id 同步加查重。docState 单测覆盖
- **[P1-2] ✅ 已修复** when 多行输入无法换行（受控值过滤空行导致回车被还原）→ 新增 LinesField：本地编辑、失焦提交（空行过滤在提交时进行）
- **[P1-3] ✅ 已修复** 题目 id 输入每键失焦（key 含 q.id 随输入变化重挂载）→ key 改用索引；dataTexts/dataErrs 草稿改按题目 id 键控
- **[P2-1] ⏳ 记录** ActionList 参数 JSON 无本地草稿态（中间态非法被还原）——MVP 限制，随 1C 编辑器迭代
- **[P2-2] ✅ 已修复** 空白模板 flow.pass 引用 v.score 但变量未声明且无计分规则 → 模板默认 `variables:{score:0}` + 示例计分规则（level:correct +10），新建关卡即可通过装载校验
- **[P2-3] ✅ 已修复** 拖拽无 pointercancel 清理（中断后 dragRef 残留）→ onPointerCancel 挂接
- **[P2-4] ✅ 部分补齐** docState 纯函数单测 4 例（id 唯一性回归/模板可装载/副本语义/题目更新）；页面级渲染测试扩充随 1C 迭代
- **[P3] ⏳ 记录**：手输 URL 可编辑内置关卡（保存将翻 builtIn=0——建议保存前拦截）、JsonTab 应用可改 doc.id、savedTip 失效策略、tryRun 首存闪烁、actionKind 混合键判定序、拖动仅下界 clamp、Inspector allIds 死参数

## 已核查无问题（摘要）

拖拽多指不串组件（dragRef.id 守卫）；record/doc 双状态防覆盖编辑；Tab 切换状态保留；copyForEditing structuredClone 安全且 id 字母表与 schema 精确匹配；保存校验链；「编辑副本」路径不可能覆盖原记录；when 拆分与引擎 AND 语义一致；规则/动作删除索引正确；新建/副本均先入库后跳转。

## 本阶段验证

- vitest **120/120**（新增 docState 4 例：id 唯一性回归、模板可装载、副本语义、题目更新）
- tsc strict ✅ · vite build ✅
- 浏览器验收：新建关卡 → 编辑器加载（面板 9 组件/标题可编辑）→ 面板添加 4 组件 → JSON 视图灌入完整配置（含规则与两道题目）→ 应用生效（画布组件同步）→ 保存（已保存 v0.1.0）→ 试运行（第 1/2 题出题正常）→ 连答两题 → 结算通过 → progress 表归档（attempts 1 / passed 1）

## 验收后 UI 修复（用户反馈）

- **画布被挤成 126px**：提示文字 `<p>` 作为 flex 子项按内容宽度（约 700px）占据了整行，挤压 `flex:1` 的画布 → `flex-wrap: wrap` + 提示 `flex-basis: 100%` 独占一行；画布 `min-width: 0`。修复后画布约 1048px 作为主体。
- 编辑器工作区加宽：`.app:has(.editor) { max-width: 1360px }`（其余页面保持 960px）。
- **favicon 404**：新增内联 SVG data-URI 图标（🎵），不再发起网络请求。
