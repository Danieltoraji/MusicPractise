# 阶段 2-2a 评审报告（节点图只读视图）

> 日期：2026-09-19 · 范围：`62c0aa4..032159e`（节点图组件/页面/Tab + 评审修复）
> 方式：subagent 聚焦 code review + 浏览器验收（听音点击关卡 44 节点/42 边完整渲染、MiniMap/Controls、缩放平移）。
> 结论：**可合入，无 P0/P1**；3 个 P2 工程性建议已修复或记录，P3 记录在案。

## 总评

programToGraph 纯函数转换正确（事件去重/边去重/触发环安全/变量读写边）、只读语义干净（不可拖/不可连）、dagre 布局正确、ReactFlowProvider 层级正确、样本关卡规则与图形一致。

## 发现与修复状态

- **[P2-1] ✅ 已修复** GraphPage 静态导入 LogicGraph 导致 EditorPage 的 lazy 失效（INEFFECTIVE_DYNAMIC_IMPORT，RF+dagre 全进主包）→ App 对 GraphPage 改路由级 lazy + Suspense，分包恢复
- **[P2-2] ✅ 已修复** `.lg-node` 无 overflow 约束，长参数文字溢出盒外 → overflow:hidden
- **[P2-3] ✅ 已修复** GraphPage 非 level 资源提示文案误导 → 区分「不是关卡」与「不存在」
- **[P3] ⏳ 记录**：emit 到无人监听事件不画边（有意：图表达"活的"触发链，建议 lint 层补死 emit 提示）；programToGraph 的 v. 提取会把 `q.v.x`、字符串字面量内 `v.x` 误计（多画无害边）；混合键动作按 cmd 优先（与引擎一致）；重复 rule.id 无防御（编辑器生成保证唯一，1C-2 遗留已在修改队列）；LogicGraph 零渲染测试（图中编辑批次前补基础渲染测试）
- ⏳ 移入后续：fitView 150ms 定时启发式改用 RF12 正式 API（useNodesInitialized/onNodesInitialized）并仅在结构变化时重 fit（图中实时编辑批次需要）

## 已核查无问题（摘要）

runId 时序（React 保证旧 cleanup 先于新 effect，stopAllClicks/按轮 disposers 不误杀新轮）；checkExprText 纯语法检查边界；actionKind 三方键序一致；`__` 前缀命令过滤；保存拦截 builtIn 匹配与 tryRun await；JsonTab 强制保留 id 与同步；App key=hash 无误重挂载；clef alto 三处一致；rhythm-follow 每题 grid 步长与 BPM 吻合、judge 后清空 taps、失败重开带新 runId；新增依赖仅 @xyflow/react + @dagrejs/dagre。

## 本阶段验证

- vitest **139/139**（本批新增 programToGraph 9 例：事件去重/触发边/写入边/读取边/环安全）
- tsc strict ✅ · vite build ✅
- 浏览器验收：听音点击关卡节点图 44 节点（3 变量+7 事件+7 规则+27 动作）+ 42 边完整渲染，MiniMap/Controls 可用
