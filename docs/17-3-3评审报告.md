# 阶段 3-3 评审报告（LevelScript 代码页）

日期：2026-09-19 · 范围：c138dbf..ccbb599 · 结论：**P0×1/P1×2 全部修复（ccbb599），244/244 测试全绿，验收通过——「单一 IR 三视图」完整闭环达成，编辑器五子页全部接线，进入 3-4 画布增强批次**

## 1. 本批交付

- **ScriptTab 代码页**（编辑器第 3 个 Tab：画布|节点图|脚本|题目|JSON）：
  - 进入/重新生成：`generateScript`；「应用到节点图」：`parseScript` → 整体替换 logic（变量保留、节点图按脚本重建、节点 id 带 `s` 前缀可辨识来源）
  - 解析失败显示**行:列 + 消息**且 doc 不变；Tab 键插入两空格；编辑即清除旧错误定位
  - **草稿跨 Tab 缓存**（模块级 `drafts Map<docId, draft>`）：编辑器 Tab 条件渲染切走即卸载，草稿必须跨 Tab 存活
  - **陈旧判定**：挂载时比对缓存 `baselineSig` 与当前 IR 签名（`signatureOf` 剔除位置字段）→ 黄条双按钮「重新生成（对齐节点图）/仍要应用当前文本（覆盖节点图）」
- `EditorPage`：script Tab 接线；**Tab 内容外包 ErrorBoundary**（编辑器路由此前无任何边界）
- 测试：渲染生成/应用回写（变量保留+s 前缀）/语法错误行列且 doc 不变/草稿跨 Tab 存活+黄条双按钮/陈旧干净禁用应用/非结构化图降级不白屏/Tab 键缩进

## 2. 评审发现与修复（ccbb599）

| 级别 | 问题 | 修复 |
| --- | --- | --- |
| P0 | **非结构化图进脚本页整树白屏**：generateScript 对「多路径汇入/循环体不回头」抛错，而 useState 初始化与 regenerate 均未设防；编辑器路由无 ErrorBoundary → React 卸载整个 root，未保存编辑全丢。此类图在节点图 GUI 可正常搭出（connect 不限扇入），JSON 页直贴也无校验 | `genDraft` 包装 try/catch（genError 状态）；渲染可读降级错误卡（「当前逻辑图超出伪代码能表达的结构——请在节点图中简化」）并禁用编辑与应用；regenerate 同样设防；**EditorPage Tab 内容外包现成 ErrorBoundary** 兜住未来所有子页；P0 回归测试（汇合图构造）钉死 |
| P1-1 | **「陈旧+干净」状态黄条不出现但应用可点**：用户看过脚本页（生成基线）→ 节点图改逻辑 → 回脚本页（未打字）→ 无任何提示但 primary 按钮可点，单击即静默覆盖新 IR——黄条机制被绕过 | 黄条条件改为 `irStale`（不限 textDirty，文案区分两种状态）；「陈旧且文本干净」时**禁用应用**（applyDisabled）——须先编辑或重新生成；用例钉死 |
| P1-2 | 头牌特性零断言：陈旧黄条双按钮、草稿跨 Tab 存活均无测试；模块级 drafts 缓存跨测试耦合 | 用例 4 重写：三段断言（草稿保留/黄条双按钮文案/重新生成后对齐且黄条消失）；新增「陈旧+干净禁用」「非结构化降级」「Tab 键」用例；各用例唯一 doc.id 解除缓存耦合 |
| P2 已修 | parseError 不随编辑清除；irStale 永不复位（干净文本上的「无操作应用」会全量改写节点 id 并标脏 doc）；渲染期写模块 Map；非 ScriptError 兜底显示「第 0 行第 0 列」 | 编辑即清错误定位；irStale 加 setter（regenerate/apply 成功复位）；drafts.set 移入 useEffect；0/0 时省略行列前缀 |
| P2 记录 | 应用成功的 lint 反馈在保存后才以黄条出现（非红卡，已知 UX 缺口）；drafts Map 跨「离开编辑器再进入」仍生效（未在 UI 标注，内存 KB 级可忽略）；signatureOf 只有安全方向假阳性（多弹黄条）；dirty-guard（未保存编辑离开提示）待办 3-4 前 | 记录在案 |

## 3. 验证

- `tsc --noEmit` 干净；`vitest run` **244/244**（+3）；`npm run build` 通过
- 浏览器验收：内置关卡进入脚本页生成正确脚本；编辑追加处理器（wait + emit）→「应用到节点图」→ 切节点图确认 wait/emit 节点出现（30 节点同步）；语法错误 `v.x = ` → 「解析失败：第 47 行第 13 列——赋值缺少右侧表达式」且 doc 不变；**草稿跨 Tab**——脚本页打字 → 节点图删边（外部 IR 变更）→ 回脚本页 → 草稿保留 + 陈旧黄条 + 「仍要应用」覆盖生效；**运行日志完整链**——新建关卡落模板、branch 条件改 `1 +`（lint 警告不拦保存）、试运行触发 `表达式意外结束`、sessionStorage 记录 `{error, nodeId:'n6'}`、编辑器日志面板点击定位 n6 并选中（红卡+field 详情）

## 4. 结论

**3-3 验收通过。** 「单一 IR 三视图」完整闭环达成：节点图（结构编辑+运行日志定位）、脚本页（文本编辑+解析应用+陈旧保护）、JSON（直改）共享同一 GraphProgram，结构化子集内三视图无损互转（往返恒等 golden 持续守护）。**进入 3-4（画布增强：真组件预览、props 结构化表单、画布缩放可选）**——首个顺手项为编辑器子页的 dirty-guard（未保存编辑离开提示，评审记录在案）。
