# 阶段 1B-2 评审报告（系列浏览页 + 进度存档）

> 日期：2026-09-17 · 范围：`b460336..HEAD`（2 提交：1B-2 功能批 + hooks 顺序修复）
> 方式：subagent 聚焦 code review + 浏览器验收（系列页结构、进度写入、徽章持久化）。
> 结论：**可合入，无 P0**；1 个 P1 与 3 个 P3 已在本批修复（见文末状态）。

## 总评

hooks 顺序修复正确、Dexie v1→v2 升级路径安全（仅声明新表，老库自动加 progress）、进度存档语义（历史最高分/通过保持/次数递增）无误；系列结构化浏览（系列→专题→关卡）全链路可用。

## 发现与修复状态

- **[P1] ✅ 已修复** `ensureSeeded` 无条件 bulkPut 会把用户导入的同 id 异版本文档静默覆盖回内置版本（且 builtIn 翻回 1，注释与行为相反）。修复：先 bulkGet 检查，只补"缺失或仍为内置"的记录；新增 P1 回归测试（导入 2.0.0 → ensureSeeded → 保持 2.0.0 且 builtIn=0）
- **[P2] ✅ 已修复** SeriesPage 对 `tdoc.content.levelIds` 直接断言，topicIds 误指非 topic 记录时整页白屏 → 改用 childIds（自带数组防护）+ 页面包 ErrorBoundary；专题缺件（bulkGet undefined）从"静默消失"改为可读缺件行
- **[P3-1] ✅ 已修复** HomePage 首帧闪现"所有关卡都已归入系列"（useLiveQuery 默认值 [] 与"查询完成但为空"混淆）→ 默认值改 undefined + loading 分支
- **[P3-2] ✅ 顺带修复** 空专题的文案缺失 → "该专题没有关卡"提示行
- ⏳ 移入后续：refs.version semver 范围校验（阶段 2）、页面级 jsdom 渲染测试扩充（1C 编辑器基建一起做）、manifest.json 交叉核对

## 已核查无问题（摘要）

Rules of Hooks 三页合规（含 adf35fc diff 确认）；Dexie v1→v2 老库升级；LevelRunner session 稳定性（useCallback[id] 稳定引用，运行中不重建）；saveProgress 事务语义（max/通过保持/attempts+1）；双写一致性（refs ∪ content.topicIds/levelIds 闭包并集不漏检）；bulkGet 空数组与线性查找规模安全。

## 本阶段验证

- vitest **116/116**（新增：进度存档 3 例、浏览分区 3 例、seed 不覆盖导入 1 例）
- tsc strict ✅ · vite build ✅
- 浏览器验收：清库首启 seed 8 份（5 关卡+入门系列）；首页「系列」+「独立关卡」分区正确；系列页专题分节与 5 关卡行完整；连答 3 题结算（30 分通过）→ progress 表正确写入 → 系列页「✅ 通过」徽章 → 重载后保持

## 过程记录（诚实归档）

1. 本轮验收中发现并修复一个我自引入的 bug：LevelPage 的 useCallback 放在条件早退之后，违反 Rules of Hooks，导致直接以 URL 打开关卡页整页空白（adf35fc）。
2. 排查过程中的大绕弯：手打 ULID id 长度出错（30 位真实 id vs 28 位手打变体）+ Vite 在 Windows 下对 LevelRunner.tsx 的 HMR 失效（浏览器持续运行旧模块，dev server 重启后才生效）——两者叠加掩盖了真实状态。教训：**涉及模块行为的浏览器验证必须重启 dev server 或强制刷新模块；所有 id 必须从文件/库中程序化获取，严禁手抄**。
