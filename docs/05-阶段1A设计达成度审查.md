# 阶段 1A 设计达成度审查 + 第三轮代码评审

> 日期：2026-09-17 · 范围：设计意图达成度（对照原始需求）+ 未评审提交 `486784f..ab86fcc`
> 方式：subagent 代码评审 + 设计差距分析（设计意图审查子代理连续三次被内容安全过滤器误拦，该部分由主代理基于完整上下文直接完成，均有代码证据）。
> 结论：**阶段 0 + 1A 设计意图已达成；无 P0/P1/P2 缺陷；3 个 P3 加固项已于本批修复（见文末状态）。**

## 一、设计意图达成度矩阵

原始需求三条主线 + 路线图口径：

| 条目 | 状态 | 证据 |
|---|---|---|
| 层级：题目（纯数据 + 每题注入） | ✅ 达成 | `schemas/v1/level.json` Question；bindings `$q.*` 注入（LevelSession.loadQuestion） |
| 层级：关卡（组件+逻辑+题目） | ✅ 达成 | 五个示例关卡全 JSON 驱动跑通全链路（浏览器实测） |
| 层级：系列/专题 | 🔜 1B | schema 就绪（series/topic.json）；运行时无浏览页/装载路径——目前只是数据结构，属路线图内 |
| 全层级 UGC：创作 | 🟡 手写 JSON（编辑器在 1C）；校验/ lint /降级管线已备 |
| 全层级 UGC：保存/分享 | 🔜 1B（Dexie + 导入导出） |
| 音乐组件：staff / sound / fingering | ✅（fingering 为键盘简化版，instrument 档案绑定待 1B 资源库） | src/components/views.tsx |
| 音乐组件：drums / tuner | 🔜 阶段 2 组件化（技术已由 RhythmDemo/TunerDemo 页验证） | src/pages/ |
| 交互组件：label/button/slider/input/choice | ✅ 全部落地 | componentDef.ts + views.tsx |
| 隐形组件 | ✅ | `visible:false`（sound1/timer1 实际使用） |
| 统一接口 | ✅ | ContractDoc events/commands/state/bindings + 注册表 |
| 节点图或简易代码 | ✅ 简易代码（ECA+安全表达式 v1.1）；🔜 节点图为阶段 2（React Flow 视图） | src/engine/ |
| 阶段 0 验收标准 | ✅ 复核成立（代码/测试证据，非文档自述） | 86 测试、五关卡浏览器实测 |
| 阶段 1A 验收标准 | ✅ 复核成立（三个新关卡 + 契约/schema/docs 同步） | docs/04 |

UGC 两个闭环缺口（均为路线图内）：**创作**（现只能手写 JSON，编辑器在 1C）、**分享**（导入导出在 1B）。

## 二、第三轮代码评审（`486784f..ab86fcc`）

总评：**可合入**。效果通道重构方向正确。

### 效果通道专项（四维，全部通过）
- **重入**：sink 同步执行于 bump 之后；host 只排程不回流；`__tick` 无效果——同步重入深度 1，无递归面
- **双执行/零执行**：规则链路与视图链路各恰好一次（回归测试覆盖）；生产构造即注册 sink，早于 start()
- **泄漏**：sink 为 per-session store 字段；key 重挂载走新 store 新 sink，旧 sink 随旧树 GC；dispose 后旧 store 无人调用
- **时序**：先 bump 后执行（效果读到新状态）；strike 先于判定音效（"先听后判"）

### 发现与修复状态
- **[P3-1] ✅ 已修复** lint 不合并 logicPatch.variables → 三个示例关卡每次装载误报 `set 未声明变量 "answered"`。修复中发现更深一层：`{set, expr}` 动作同时含 `expr` 键，原分支顺序使 set 检查**永不可达**（此前"补齐 set 未声明检查"实为死代码）——已重排分支使检查真正生效，`LevelSession` 传入各题 patch 变量键并集豁免。测试：extraVariableKeys 豁免 + 未声明报出
- **[P3-2] ✅ 已修复** 规则内 `level.restart` 在级联中途重入：旧队列事件会作用于重置后状态。修复：`dispatch` 引入代数计数，`reset()` 递增代数、级联循环检测到代数变化即作废剩余队列。测试：restart 后 e3 不再触发
- **[P3-3] 判定为设计允许，不修**：melody-dictation 答错可重试再得分——`answered` 锁定保证每题至多计分一次（总分上界 30，无放大），属练习语义；转评估场景再加首试限制
- **[P3 注记] strike 与判定音效并发**：两次独立 playNotes 无互踩；同音高时约 60ms 双 attack 轻微 flanging，可接受

## 三、验证状态

- vitest **86/86**（新增：extraVariableKeys 豁免、级联 restart 队列作废、视图 strike 效果达宿主）
- tsc strict ✅ · vite build ✅
- 此前浏览器实证：音频层探针（C4/E4 不同采样缓冲）、五关卡判定链路、answered 防刷分
