# 阶段 2-1 评审报告（rhythm/tuner 组件正式化）

> 日期：2026-09-17 · 范围：`63088f9..HEAD`（2 提交：组件正式化功能批 + tap 相对时间修复）
> 方式：subagent 聚焦 code review + 浏览器验收（节奏关自动开始/节拍动画/tap 判定/失败自动重开；调音关 mic 拒绝路径）。
> 结论：初版发现 **1 个 P0**（rhythm 视图 effect 不重排导致失败重试与第 2 题双双软锁）——已修复并在浏览器回归确认；其余 P1/P2 一并处理。

## 总评

组件契约设计清晰（rhythm/tuner 只翻状态不携带 effects，时序逻辑全部内聚在视图/session），rhythmScore 表达式函数类型校验完备；TunerView 生命周期严谨（alive 竞态、拒绝 catch、清理完整）。P0 软锁由视图 effect 依赖设计缺陷引起——runId 代数修复后已回归通过。

## 发现与修复状态

- **[P0] ✅ 已修复** RhythmView effect deps 为原始值（running/beats/bpm/spec.props/emit）：roundDone 后 judged.else 重发 start（参数相同）→ deps 不变 → effect 不重跑 → 失败重试无声、第 2 题 roundDone 恒不触发（软锁）。修复：RhythmState 增加 `runId`（每次 start +1）加入视图 deps；失败自动重开已在浏览器确认（active 拍点再现）
- **[P1] ✅ 已修复** rhythm-follow q2 题面 104 BPM 但节拍器固定 80 → 每题 data.bpm，load/judged.else 均从 `$q.data.bpm` 取用
- **[P2-1] ✅ 已修复** 已排程节拍音在 stop/换题/卸载后仍会响完 → audio.ts 登记 activeClicks，stopAllClicks() 挂入视图清理
- **[P2-2] ✅ 已修复** countInBeats 契约参数未生效 → applyCommand 读取并入 state.countIn，视图改用 state.countIn
- **[P3] ⏳ 记录**：TunerView 麦克风拒绝后需 stop→start 才能重试；tap 的 120ms 闪效 setTimeout 未清理；预备拍期间 beat-dot 无视觉反馈；tune-intro 未校验目标音高（题面承诺）；stable 按帧计数与"保持 1 秒"语义有偏差
- ⏳ 测试盲区：RhythmView/TunerView 无组件测试、levelSession 无 rhythm 链路测试——列入 1C 迭代

## 已核查无问题（摘要）

tap 相对时间修复（t0 与 grid 同基准、预备拍为负）；rhythmScore 类型校验完备、tol ms→s 语义正确、与 rhythmMatch 贪心匹配一致；judge 链顺序（emit payload 入队时快照、先算分后清 taps——顺序 bug 由本轮验收发现并修正）；同事件规则声明序（in-tune 先于 passed）；logicPatch 每题重置 stable/scored；level.restart 走 resetAll 可正常重入；TunerView alive 竞态/拒绝 catch/清理正确；编辑器兼容（palette/规则枚举自动纳入新组件）；db seed 11 份与测试一致。

## 本阶段验证

- vitest **121/121**（新增：rhythmScore 接入表达式 4 例 + docState/store 增量）
- tsc strict ✅ · vite build ✅
- 浏览器验收：节奏关自动开始、节拍动画、失败自动重开新一轮（P0 回归）、对齐打拍判定通过（契合度 63% 计分 6）、下一题解锁；调音关渲染与 mic 拒绝提示路径正常（真实调音需人工）
