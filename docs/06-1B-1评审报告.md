# 阶段 1B-1 评审报告（资源库与分发 · UGC 主干）

> 日期：2026-09-17 · 范围：`69326a5..b460336`（5 提交：deps / Dexie 库 / 校验+装载管线 / 导入导出 / App 切库）
> 方式：subagent 聚焦 code review + 浏览器验收（清库首启 seed、资源库页、库内试玩）。
> 结论：**可合入，无 P0/P1**；3 个 P2 与若干 P3 已在本批修复（见文末状态）。

## 总评

管线设计（信封校验 → 包内去重 → refs 闭包 → 单态冲突策略）与 docs/02 §7 一致，测试覆盖扎实（库 6 / 装载 7 / 导入导出 8 / schema 15+）；架构性风险（导入校验逃逸、闭包防环、事务半提交、seed 竞态）经专项核查全部闭合或已修复。

## 导入管线健壮性专项

1. **zip 炸弹**：原 `unzipSync` 无上限 → ✅ 已修：条目 50MB / 整包 100MB 解压上限
2. **路径穿越**：zip 条目名仅作数据过滤键（`resources/` 前缀 + `.json` 后缀），无文件系统写入 → 无 zip-slip 面
3. **超大/坏 JSON**：✅ 已修：逐条目 try/catch 进 `parseErrors`，作为被拒条目进报告；整批不再因单条崩溃
4. **refs 指向被拒条目**：被拒条目不入池；若库内有同 id 则按库版本闭包成立（正确），否则 missing → 整包拒绝（正确）
5. **事务性**：✅ 已修：导入写入包进 Dexie `transaction('rw')`，不半提交；重试本就幂等（同 version 跳过）
6. **双标签 seed 竞态**：count-check + bulkPut upsert，双双 seed 写同数据，良性
7. **useLiveQuery / ErrorBoundary 时序**：deps 正确；边界随路由卸载自然重置

## 发现与修复状态

- **[P2-1] ✅** zip 炸弹无防护 → 解压上限 + 测试（坏条目进 parseErrors）
- **[P2-2] ✅** 导入无事务 → Dexie 事务包裹，不半提交
- **[P2-3] ✅** `loadLevelDoc` 全应用无调用点（管线死代码，LevelPage 直接 `as LevelDoc`）→ LevelPage 装载时执行：未通过展示可读错误列表拒绝运行；lint 警告折叠展示
- **[P3-1] ✅** 单条坏 JSON 使整批导入崩溃 → per-entry 解析错误进 rejected 报告
- **[P3-2] ✅** 导入按钮无 in-flight 禁用（连点重复计数）→ importing 状态禁用
- **[P3-3] ✅** App seedError 为终态 → 加重试按钮
- **[P3-4] ✅** HomePage 卡片无边界包裹（损坏 doc 白屏首页）→ 卡片网格包 ErrorBoundary
- **[P3-5] ✅** `addedDocs` 死代码与 io.test 空包用例意图混乱 → 清理并补坏条目混合用例
- **[P3-6] ✅** docs/02 §7 补记：zip 防护、事务、LevelPage 接线
- ⏳ 移入后续：manifest.json 交叉核对（装饰性）、refs.version semver 匹配（阶段 2）、`as never` 断言收窄为信封类型（1C 编辑器）

## 已核查无问题（摘要）

闭包防环（complete 集合）；导出自包含缺件拒绝；slugify 边界；`Uint8Array as BlobPart` 兼容；schemaVersion 导入期拦截；XSS 面为零（无 dangerouslySetInnerHTML，React 文本转义覆盖）；双标签 seed 竞态良性；schemas.test 迁移共享 buildAjv 并补系列/专题用例；提交拆分清晰。

## 本阶段验证

- vitest **109/109**（库 6 / 装载 7 / 导入导出 9 / 引擎+表达式+节奏 33 / schema 18 / 组件 8 …）
- tsc strict ✅ · vite build ✅
- 浏览器验收：清库首启自动 seed 5 关；资源库页列表正确（内置禁删、导出/试玩可用）；从库加载关卡经装载管线正常出题；首页卡片渲染导入标记
