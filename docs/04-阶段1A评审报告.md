# 阶段 1A 评审报告（表达力扩展）

> 日期：2026-09-17 · 方式：独立 subagent 聚焦 code review（范围 `01bccb3..486784f`，5 提交 18 文件 +1221/-36），
> 关键结论经动态验证（临时脚本实测计时器泄漏/重 arm 链等）与复跑（tsc / vitest 80/80 / build）确认。

## 总评

**有条件可合入**（随后本批修复已落地，见文末状态）：表达式安全边界延续且完备、组件契约纯函数化贯彻一致、`__` 内部命令约定落实；但发现 1 个 P1——本阶段新写的 docs/02 计时器契约（"换题自动清理"）与实现不符，实测确认计时器跨题泄漏，属"契约声明与行为脱节"模式的再次出现，修复一行。

## 发现与修复状态

### P1 ✅ 已修复
- **换题不清计时器**：`loadQuestion` 无 timer 清理，logicPatch arm 的计时器跨题泄漏触发 tick（实测：换题后 200ms 内继续 tick 7 次）。
- 修法：`loadQuestion` 开头 `stopAllTimers()`；新增 fake-timer 回归测试（换题后 ticks 不再增长）。

### P2 ✅ 全部修复
1. **note-spelling 答对后可重复提交刷分**（示例即模板，危害放大）→ 三个示例关卡统一引入 **answered 守卫范式**：每题 `logicPatch.variables` 重置 `answered:false`；判定规则 `when` 首条件 `!v.answered`、命中即置 true；对/错拆两条规则（`when` 失败会走 `else`，守卫不能与 else 混用——范式已写入 docs/02 §6-19）
2. **timed-reaction 超时与点击的 React 提交窗口竞态** → 同一 answered 守卫在规则层兜底（timeout/correct/wrong 三规则互斥）
3. **timer.start 重置 count 的链式重 arm 陷阱**（实测 300ms tick 10 次无终止）→ docs/02 §6-18 记载语义与正确写法（外部变量计数）
4. **InputView 回车未过滤 IME 组合态**（zh-CN 场景确认候选即误提交）→ `!e.nativeEvent.isComposing` 守卫
5. **engine.reset() 整体替换 vars 对象** → 级联中途 restart 后同批 set 动作"读旧写新"（实测 score 丢失/after 错乱）→ 改为原地清空回填保持对象身份，补身份回归测试

### P3（本批修复 + 遗留）
- ✅ tempo 钳位 [20,300]（防 Infinity 排程）；timer ms 上界 24h；mode 未知值告警
- ✅ schema：`logicPatch.variables` 与 `logic.variables` 同步放行标量数组
- ✅ slider `setValue` 按 min/max 钳制（状态与显示一致）；移除 FingeringView 无消费者的 `data-clear`
- ✅ docs/02：契约表 timer 行补 count 语义、sound.stop 标注 v1 未实现、"后续组件"清单更新、§6-17 措辞澄清（128 上限仅约束初值）
- ✅ 新增 FingeringView jsdom 冒烟测试（黑白键数量/data-midi/点击/高亮）
- ⏳ 移入后续阶段：sound.stop 真实实现（需 play 句柄管理）、`trim` 函数、数组尾逗号/科学计数法下标等解析便利性、SliderView 原地点击去重

## 已核查无问题（摘要）

表达式数组/下标的安全与预算（节点计数、2000 字符 + 深度 48 解析期拦截、下标仅收数字、越界 null）；计时器句柄管理（重复 start 替换句柄、clearTimeout 兼容 interval、dispose/StrictMode 路径）；seq 排程与 mode/tempo 全链路透传；Slider/Input 直调 store 的受控输入模式合法；契约表 16 函数与实现逐一吻合；三关卡 schema 校验；timed-reaction 主链路两个有序方向安全。

## 本阶段测试与验证

- vitest **82/82**（新增：换题清理计时器、reset 对象身份、FingeringView 冒烟、表达式数组/下标边界、timer 单次/重复/stop/dispose）
- tsc strict ✅ · vite build ✅
- 浏览器实测：旋律听写（错序判定失败并清空 → 正确序列 C-D-E 得分 10 解锁下一题）、限时反应（3 秒超时 → ⏱ 超时！→ reveal → 解锁）、音名拼写（小写 "ceg" 回车 → upper() 判定通过得分）
- 声音可闻性（velocity 修复后）仍需人工试听确认
