# 19 · 3-5 批次评审报告（节点图友好化）

> 批次目标（用户指令）：① 节点图各节点结构化 UI——除变量名/路径选择外不让用户写代码；② 节点图页角落展示画布对照图（组件+名称），方便接线对照；③ 自主迭代不提问。
> 评审方式：subagent 独立评审（含 tsc / vitest / build / 8 内置关卡全管线探针 / exprBridge 边界实证），主会话修复后回归。
> 基线：281/281 测试全绿、tsc 0 错误、build 通过（278→281，新增 exprBridge 边界 2 例 + P1-1 回归 1 例）。

## 1. 批次交付物

| 项 | 内容 | 提交 |
| --- | --- | --- |
| F1 结构化编辑地基 | `exprBridge.ts`（表达式↔Operand/Cond/ArgView 纯函数双向桥，解析失败一律回落 advanced 保原文）、`controls.tsx`（OperandPicker 数字/文本/开关/引用/表达式五模式、ConditionBuilder 比较/真值/高级、CallArgsEditor 契约键值传参、QueryCallEditor）、`summary.ts`（节点卡中文摘要翻译） | 17cd719 |
| F1 Inspector 重做 | NodeInspector 按 kind 全结构化：on 事件分组下拉（关卡/组件/内部）、call 实例友好名下拉+动作下拉（契约命令∪基座命令，去重）+按契约参数键值+「按契约补全」、assign 变量下拉+值六模式、branch/loop 条件构造器、wait/emit/payload 全控件化；`key=node.id` 重挂载隔离草稿；高级模式兜底且原文不丢 | f2e527a |
| F2 画布对照小图 | `CanvasMap.tsx` 角落面板：组件框按布局等比缩放+友好名+类别描边+幽灵虚线；点击组件 → relatedIds 高亮图中引用它的节点（on 事件前缀/call 目标/assign 查询）；工具条开关 | f2e527a |
| F3 孤儿 lint 防刷屏 | 从 on 可达性分析 + 弱连通分量归组：链只报链头、纯环/悬挂环合并为一条孤岛告警（docs/16 遗留 P2 收口）；旧「无入边即报」是严格子集，8 内置关卡零误报 | 0f13429 |
| 节点卡中文摘要 | `当 重听·被点击`/`发声器·play { notes }`/`如果 v.score >= 10`/`重复 3 次`/`等待 500 毫秒`/`level·next` | f2e527a |

## 2. 评审结论：可合并（无 P0）

工程面：tsc 零错、281 用例全绿、build 成功、8 内置关卡经 v1→v2 迁移 + lint 全管线零误报；datalist id 唯一、getDef 未知类型双保险、engine 无 DOM 访问面。两条 P1 均为「一次交互不可逆丢用户输入」，与本批「原文不丢」目标冲突，已在本批修复。

## 3. P1（已修复，fb7068f）

| # | 问题 | 修复 |
| --- | --- | --- |
| P1-1 | assign「查询⇄表达式」往返丢原表达式：点查询当场销毁 expr，再点 ƒx 落 `'0'` | RValueEditor 以 `lastExprRef` 记住进入查询前的表达式，离开查询模式即恢复；新增 jsdom 回归用例（as3：v.score + 7 往返恒等） |
| P1-2 | call 换绑同类实例（方法实际未变）也无条件重置 args，已填参数被清 | `method === node.method` 时只 patch target，保留 args；方法真变才按新契约重置 |

## 4. P2 处置

**本批修复**：
- P2-2 顶层含 `&&/||/?:` 不再拆比较（此前 `v.a == 1 || v.b == 2` 被错分组显示），括号内逻辑符不影响——`hasTopLevelLogical` 守卫 + 2 个边界用例
- P2-4 emit 负载键改名补重名守卫（与 CallArgsEditor 行为对齐：空名/重名还原输入框）
- P2-5 RValueEditor 复用 controls 的 `defaultOperandExprFor`（消除双实现漂移）；无组件时禁用「查询」按钮
- P2-6 工具条收起画布对照即清 mapFocus（高亮不再残留）
- P2-7 rfNodes 签名并入 compsSig（组件改名即失效节点卡缓存；当前图页不改组件，防患未然）
- P2-8 非字符串 args 用 JSON.stringify 序列化（不再 `[object Object]`）
- 方法下拉去重：契约已声明的基座命令（如 button.setEnabled）不再重复出选项（浏览器验收发现）

**记录在案（按需后置）**：
- P2-1 结构化模式一键切换即提交默认值（无确认无撤销）——当前模型为「离散动作即时生效+卡片可见」，完整解法依赖撤销/重做栈（docs/12 出界项），届时一并做「切回恢复上一值」
- P2-3 单引号字符串往返被规范化为双引号（值不变源文本改写）——已加用例钉住行为与注释
- CanvasMap 极端 w/h=0 组件框可能溢出面板边框几像素（纯视觉）

## 5. 浏览器验收（单标签页，localhost:5200）

- 节点卡中文摘要全量正确（含分支真/假双出口、幽灵 sound 组件）
- 画布对照图：7 组件框渲染+名称+焦点描边；点击「发声器」→ 3 个相关节点 `is-related` 高亮
- on 事件分组下拉选择即提交；call 实例「下一题（nextBtn）」友好名下拉 + `{ enabled: boolean }` 契约提示 + 按名传参键值 + 开关控件编辑 → `💾 保存 *` 脏标记即时出现
- branch 复杂条件 `(event.midi == q.data.answerMidi)` 正确回落「真值+高级」且原文完整

## 6. 结论与下一步

**3-5 验收通过**：节点图达成「不写代码」目标——事件/实例/方法/变量全部下拉，字面量走类型化控件，参数按契约键值传参，表达式仅高级兜底且原文不丢；画布对照图解决接线时的跨页对照痛点。
后续候选：① 编辑器继续打磨（撤销/重做栈——P2-1 的完整解法、staff 预览 bindings 注入、props 交叉校验等 docs/18 遗留）；② 新功能方向（MusicXML 导入、关卡分享增强）。
